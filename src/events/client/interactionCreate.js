// Slash command and button interactions are handled in interactionHandler.js
// This event file is intentionally left as a pass-through to avoid duplicate listeners.
module.exports = {
  once: false,
  run() {},
};
